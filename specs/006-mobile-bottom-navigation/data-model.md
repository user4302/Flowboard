# Data Model: Mobile Bottom Navigation

No new data entities required.
- **View State**: Uses existing `currentView` in `uiStore`.
- **Navigation State**: New UI state for active filter sheet (`showFilterSheet`) in `uiStore`.
